# --- Application ---
# Clé secrète pour signer les JWT (NE PAS utiliser cette valeur en prod)
# Génère une vraie clé :  python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=changeme-super-secret-key
ACCESS_TOKEN_EXPIRE_MINUTES=60

# --- Base de données (Neon / Supabase / etc.) ---
# IMPORTANT :
# - ALEMBIC_DATABASE_URL = URL "directe" (connexion sync psycopg2) → pour les migrations Alembic
# - DATABASE_URL         = URL "pooled"  (connexion async asyncpg via PgBouncer) → pour FastAPI en runtime
#
# Neon fournit 2 hosts :
#   - HOST_POOLED  (PgBouncer, mode transaction) → pour l'app
#   - HOST_DIRECT  (Postgres direct)             → pour alembic/psql/pg_dump
#
# Garde ?sslmode=require

# URL sync (psycopg2) pour Alembic — connexion DIRECTE (sans pooler)
ALEMBIC_DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@HOST_DIRECT:5432/DBNAME?sslmode=require

# URL async (asyncpg) pour l’app FastAPI — via le POOLER (PgBouncer)
DATABASE_URL=postgresql+asyncpg://USER:PASSWORD@HOST_POOLED:5432/DBNAME?sslmode=require

# --- Seed (facultatif) ---
SEED_ON_START=false

# --- Debug (facultatif) ---
DEBUG=true

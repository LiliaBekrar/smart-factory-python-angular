# --- Application ---
# Clé secrète pour signer les JWT (NE PAS utiliser cette valeur en prod)
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=changeme-super-secret-key
# Durée de vie du token d'accès (minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=60

# --- Base de données ---
# Exemple de connexion Postgres (remplacer par votre URL Render/Neon/Supabase)
# Format: postgresql://user:password@host:5432/dbname
DATABASE_URL=postgresql://user:password@host:5432/dbname

# --- Seed (facultatif) ---
# Si true|1|yes → lance le seed au démarrage (utile en dev ou pour démo)
SEED_ON_START=false

# --- Debug (facultatif) ---
DEBUG=true
